#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 * Description: 'Prints all the letters finishing w new line'
 * Return: Always 0.
 */
int main(void)
{
	for (int i = 0; i < 10; i++)
	{
		printf("%i", i);
	}
	printf("\n");

	return (0);
}
