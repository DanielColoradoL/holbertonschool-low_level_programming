#include <stdio.h>

/**
 * main - Entry point
 * Description: 'the program prints out a message on the screen'
 * Return: Always 0.
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
